version: '3'

services:
  convert:
    build:
      context: .
      dockerfile: docker/convert/Dockerfile
    image: moorelab/krag-convert:${TAG}
    volumes:
      - "${PWD}:/data"
    working_dir: /data